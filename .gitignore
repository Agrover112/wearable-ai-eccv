# ── Heavy / regenerable data — NEVER in git (re-pulled from HF, cached on Drive) ──
data/
models/
# Ignore outputs/ CONTENTS (use /* so git still descends for the negation below)...
outputs/*
# ...but DO keep the small EDA text outputs (reports, tables, figures) in git:
!outputs/eda/
runs/
slurm_logs/
baselines/longqa/output/
*.mp4
*.parquet
*.arrow

# ── Secrets ──
.env
*.token
hf_token*
**/secrets*

# ── HF / torch caches ──
.cache/
hf_cache/

# ── Python ──
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
.conda/
*.egg-info/

# ── OS ──
.DS_Store
*.bak
visualizations/agentic_dev140_orchestration_5/assets
slurm_scripts
slurm_outputs